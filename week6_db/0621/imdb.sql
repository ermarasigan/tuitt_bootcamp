create table  (
    id int not null auto_increment,
     varchar(255) not null,
    primary key(id),
    foreign key() references ()
);

Movies
title
year
length
genre
studioName
producerCertificateNumber

create table  (
    id int not null auto_increment,
     varchar(255) not null,
    primary key(id),
    foreign key() references ()
);

StarsIn
movieTitle
movieYear
starName

create table  (
    id int not null auto_increment,
     varchar(255) not null,
    primary key(id),
    foreign key() references ()
);

MovieStar
name
address
gender


create table  (
    id int not null auto_increment,
     varchar(255) not null,
    primary key(id),
    foreign key() references ()
);

MovieExec
name
address
certificateNumber
netWorth


create table  (
    id int not null auto_increment,
     varchar(255) not null,
    primary key(id),
    foreign key() references ()
);

Studio
name
address
presidentName
